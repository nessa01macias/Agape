{
  "name": "agape-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reads a site, plans the edit, renders the video.",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.ts",
    "start": "node --env-file-if-exists=.env src/server.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@remotion/bundler": "4.0.494",
    "@remotion/renderer": "4.0.494",
    "cheerio": "^1.0.0",
    "express": "^5.1.0",
    "remotion": "4.0.494",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.13.2",
    "typescript": "~5.9.3"
  },
  "engines": {
    "node": ">=22.6"
  }
}
